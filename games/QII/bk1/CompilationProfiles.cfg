{
	"profiles": [
		{
			"name": "-bsp -vis -rad",
			"tasks": [
				{
					"parameters": "-bsp -vis -rad ${MAP_BASE_NAME}",
					"tool": "${q2tool}",
					"type": "tool"
				}
			],
			"workdir": "${MAP_DIR_PATH}"
		},
		{
			"name": "Relight Qrad",
			"tasks": [
				{
					"parameters": "-bsp -onlyents -rad ${MAP_BASE_NAME}",
					"tool": "${q2tool}",
					"type": "tool"
				}
			],
			"workdir": "${MAP_DIR_PATH}"
		},
		{
			"name": "-bsp",
			"tasks": [
				{
					"parameters": "-bsp ${MAP_BASE_NAME}",
					"tool": "${q2tool}",
					"type": "tool"
				}
			],
			"workdir": "${MAP_DIR_PATH}"
		},
		{
			"name": "-bsp -nowater -vis -rad",
			"tasks": [
				{
					"parameters": "-bsp -nowater -vis -rad ${MAP_BASE_NAME}",
					"tool": "${q2tool}",
					"type": "tool"
				}
			],
			"workdir": "${MAP_DIR_PATH}"
		}
	],
	"version": 1
}
