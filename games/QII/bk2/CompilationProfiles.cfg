{
	"profiles": [
		{
			"name": " (basic compile) -bsp -vis -rad",
			"tasks": [
				{
					"parameters": "-gamedir ${GAME_DIR_PATH} -bsp -vis -rad ${MAP_BASE_NAME}",
					"tool": "${q2tool}",
					"type": "tool"
				}
			],
			"workdir": "${MAP_DIR_PATH}"
		},
		{
			"name": "(relight rad) -bsp -onlyents -rad",
			"tasks": [
				{
					"parameters": "-gamedir ${GAME_DIR_PATH} -bsp -onlyents -rad ${MAP_BASE_NAME}",
					"tool": "${q2tool}",
					"type": "tool"
				}
			],
			"workdir": "${MAP_DIR_PATH}"
		},
		{
			"name": "-bsp",
			"tasks": [
				{
					"parameters": "-gamedir ${GAME_DIR_PATH} -bsp ${MAP_BASE_NAME}",
					"tool": "${q2tool}",
					"type": "tool"
				}
			],
			"workdir": "${MAP_DIR_PATH}"
		},
		{
			"name": " (full vis / no water) -bsp -nowater -vis -rad",
			"tasks": [
				{
					"parameters": "-gamedir ${GAME_DIR_PATH} -bsp -nowater -vis -rad ${MAP_BASE_NAME}",
					"tool": "${q2tool}",
					"type": "tool"
				}
			],
			"workdir": "${MAP_DIR_PATH}"
		},
		{
			"name": " (FullVis / No rad) -bsp -vis",
			"tasks": [
				{
					"parameters": "-gamedir ${GAME_DIR_PATH} -bsp -vis ${MAP_BASE_NAME}",
					"tool": "${q2tool}",
					"type": "tool"
				}
			],
			"workdir": "${MAP_DIR_PATH}"
		},
		{
			"name": "(full vis / rad extra) -bsp -vis -rad -extra",
			"tasks": [
				{
					"parameters": "-gamedir ${GAME_DIR_PATH} -bsp -vis -rad -extra ${MAP_BASE_NAME}",
					"tool": "${q2tool}",
					"type": "tool"
				}
			],
			"workdir": "${MAP_DIR_PATH}"
		},
		{
			"name": "(full vis / rad extra max) -bsp -vis -rad -extra -maxlight",
			"tasks": [
				{
					"parameters": "-gamedir ${GAME_DIR_PATH} -bsp -vis -rad -extra -maxlight ${MAP_BASE_NAME}",
					"tool": "${q2tool}",
					"type": "tool"
				}
			],
			"workdir": "${MAP_DIR_PATH}"
		},
		{
			"name": "(full vis fast) -bsp -vis -fast -rad",
			"tasks": [
				{
					"parameters": "-gamedir ${GAME_DIR_PATH} -bsp -vis -fast -rad ${MAP_BASE_NAME}",
					"tool": "${q2tool}",
					"type": "tool"
				}
			],
			"workdir": "${MAP_DIR_PATH}"
		}
	],
	"version": 1
}
